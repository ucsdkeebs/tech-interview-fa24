import React, { useState } from 'react';

const Counter = () => {
  
  const handleClick = () => {
    // fill with necessary logic
  };

  return (
    <button>
    </button>
  );
};

export default Counter;